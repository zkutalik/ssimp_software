#include "compression.hh"
namespace compression {
    void make_compressed_vcf_file   (std:: string compressed_out_file_name, std:: string vcf_filename) {
        (void) compressed_out_file_name;
        (void) vcf_filename;
    }
}
