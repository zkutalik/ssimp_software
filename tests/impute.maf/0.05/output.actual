file_name:gwas/GIANT_HEIGHT_Wood_et_al_2014_publicrelease_HapMapCeuFreq.txt.gz
options:: opt_window_width,options:: opt_flanking_width:	300'000 , 250'000

chrm1	   18'000'000-18'300'000
      63 # GWAS     SNPs in this window (with 250'000 flanking)
      57 # SNPs in both (i.e. useful as tags)
      31 # target SNPs (anything in narrow window, will include some tags)

chrm1	   18'300'000-18'600'000
      56 # GWAS     SNPs in this window (with 250'000 flanking)
      50 # SNPs in both (i.e. useful as tags)
      36 # target SNPs (anything in narrow window, will include some tags)

chrm2	   18'000'000-18'300'000
      73 # GWAS     SNPs in this window (with 250'000 flanking)
      61 # SNPs in both (i.e. useful as tags)
      57 # target SNPs (anything in narrow window, will include some tags)

chrm2	   18'300'000-18'600'000
      68 # GWAS     SNPs in this window (with 250'000 flanking)
      57 # SNPs in both (i.e. useful as tags)
      24 # target SNPs (anything in narrow window, will include some tags)

chrm3	   18'000'000-18'300'000
      37 # GWAS     SNPs in this window (with 250'000 flanking)
      29 # SNPs in both (i.e. useful as tags)
      21 # target SNPs (anything in narrow window, will include some tags)

chrm3	   18'300'000-18'600'000
      35 # GWAS     SNPs in this window (with 250'000 flanking)
      27 # SNPs in both (i.e. useful as tags)
      20 # target SNPs (anything in narrow window, will include some tags)

chrm4	   18'000'000-18'300'000
      39 # GWAS     SNPs in this window (with 250'000 flanking)
      34 # SNPs in both (i.e. useful as tags)
      34 # target SNPs (anything in narrow window, will include some tags)

chrm4	   18'300'000-18'600'000
      36 # GWAS     SNPs in this window (with 250'000 flanking)
      31 # SNPs in both (i.e. useful as tags)
      12 # target SNPs (anything in narrow window, will include some tags)

chrm5	   18'000'000-18'300'000
      58 # GWAS     SNPs in this window (with 250'000 flanking)
      51 # SNPs in both (i.e. useful as tags)
      40 # target SNPs (anything in narrow window, will include some tags)

chrm5	   18'300'000-18'600'000
      53 # GWAS     SNPs in this window (with 250'000 flanking)
      46 # SNPs in both (i.e. useful as tags)
      24 # target SNPs (anything in narrow window, will include some tags)

chrm6	   18'000'000-18'300'000
      54 # GWAS     SNPs in this window (with 250'000 flanking)
      42 # SNPs in both (i.e. useful as tags)
      24 # target SNPs (anything in narrow window, will include some tags)

chrm6	   18'300'000-18'600'000
      51 # GWAS     SNPs in this window (with 250'000 flanking)
      39 # SNPs in both (i.e. useful as tags)
      31 # target SNPs (anything in narrow window, will include some tags)

chrm7	   18'000'000-18'300'000
      47 # GWAS     SNPs in this window (with 250'000 flanking)
      41 # SNPs in both (i.e. useful as tags)
      29 # target SNPs (anything in narrow window, will include some tags)

chrm7	   18'300'000-18'600'000
      43 # GWAS     SNPs in this window (with 250'000 flanking)
      37 # SNPs in both (i.e. useful as tags)
      12 # target SNPs (anything in narrow window, will include some tags)

chrm8	   18'000'000-18'300'000
     105 # GWAS     SNPs in this window (with 250'000 flanking)
      81 # SNPs in both (i.e. useful as tags)
      57 # target SNPs (anything in narrow window, will include some tags)

chrm8	   18'300'000-18'600'000
      93 # GWAS     SNPs in this window (with 250'000 flanking)
      73 # SNPs in both (i.e. useful as tags)
      50 # target SNPs (anything in narrow window, will include some tags)

chrm9	   18'000'000-18'300'000
      74 # GWAS     SNPs in this window (with 250'000 flanking)
      60 # SNPs in both (i.e. useful as tags)
      46 # target SNPs (anything in narrow window, will include some tags)

chrm9	   18'300'000-18'600'000
      62 # GWAS     SNPs in this window (with 250'000 flanking)
      51 # SNPs in both (i.e. useful as tags)
      23 # target SNPs (anything in narrow window, will include some tags)

chrm10	   18'000'000-18'300'000
      35 # GWAS     SNPs in this window (with 250'000 flanking)
      27 # SNPs in both (i.e. useful as tags)
      19 # target SNPs (anything in narrow window, will include some tags)

chrm10	   18'300'000-18'600'000
      35 # GWAS     SNPs in this window (with 250'000 flanking)
      27 # SNPs in both (i.e. useful as tags)
      22 # target SNPs (anything in narrow window, will include some tags)

chrm11	   18'000'000-18'300'000
      33 # GWAS     SNPs in this window (with 250'000 flanking)
      32 # SNPs in both (i.e. useful as tags)
      25 # target SNPs (anything in narrow window, will include some tags)

chrm11	   18'300'000-18'600'000
      31 # GWAS     SNPs in this window (with 250'000 flanking)
      30 # SNPs in both (i.e. useful as tags)
      18 # target SNPs (anything in narrow window, will include some tags)

chrm12	   18'000'000-18'300'000
      53 # GWAS     SNPs in this window (with 250'000 flanking)
      46 # SNPs in both (i.e. useful as tags)
      23 # target SNPs (anything in narrow window, will include some tags)

chrm12	   18'300'000-18'600'000
      46 # GWAS     SNPs in this window (with 250'000 flanking)
      40 # SNPs in both (i.e. useful as tags)
      21 # target SNPs (anything in narrow window, will include some tags)

chrm16	   18'000'000-18'300'000
      18 # GWAS     SNPs in this window (with 250'000 flanking)
      15 # SNPs in both (i.e. useful as tags)
      19 # target SNPs (anything in narrow window, will include some tags)

chrm16	   18'300'000-18'600'000
      15 # GWAS     SNPs in this window (with 250'000 flanking)
      12 # SNPs in both (i.e. useful as tags)
       1 # target SNPs (anything in narrow window, will include some tags)

chrm17	   18'000'000-18'300'000
      25 # GWAS     SNPs in this window (with 250'000 flanking)
      22 # SNPs in both (i.e. useful as tags)
      31 # target SNPs (anything in narrow window, will include some tags)

chrm17	   18'300'000-18'600'000
      21 # GWAS     SNPs in this window (with 250'000 flanking)
      18 # SNPs in both (i.e. useful as tags)
       4 # target SNPs (anything in narrow window, will include some tags)

chrm19	   18'000'000-18'300'000
      32 # GWAS     SNPs in this window (with 250'000 flanking)
      30 # SNPs in both (i.e. useful as tags)
      23 # target SNPs (anything in narrow window, will include some tags)

chrm19	   18'300'000-18'600'000
      31 # GWAS     SNPs in this window (with 250'000 flanking)
      29 # SNPs in both (i.e. useful as tags)
      23 # target SNPs (anything in narrow window, will include some tags)

chrm20	   18'000'000-18'300'000
      44 # GWAS     SNPs in this window (with 250'000 flanking)
      38 # SNPs in both (i.e. useful as tags)
      31 # target SNPs (anything in narrow window, will include some tags)

chrm20	   18'300'000-18'600'000
      39 # GWAS     SNPs in this window (with 250'000 flanking)
      34 # SNPs in both (i.e. useful as tags)
      24 # target SNPs (anything in narrow window, will include some tags)

chrm21	   18'000'000-18'300'000
      56 # GWAS     SNPs in this window (with 250'000 flanking)
      40 # SNPs in both (i.e. useful as tags)
      38 # target SNPs (anything in narrow window, will include some tags)

chrm21	   18'300'000-18'600'000
      51 # GWAS     SNPs in this window (with 250'000 flanking)
      35 # SNPs in both (i.e. useful as tags)
      24 # target SNPs (anything in narrow window, will include some tags)

chrm22	   18'000'000-18'300'000
      55 # GWAS     SNPs in this window (with 250'000 flanking)
      49 # SNPs in both (i.e. useful as tags)
      39 # target SNPs (anything in narrow window, will include some tags)

chrm22	   18'300'000-18'600'000
      50 # GWAS     SNPs in this window (with 250'000 flanking)
      44 # SNPs in both (i.e. useful as tags)
      24 # target SNPs (anything in narrow window, will include some tags)
