file_name:gwas/fixup.UKBB.both.smry..fdr
options:: opt_window_width,options:: opt_flanking_width:	250'000 , 250'000

chrm1	   18'000'000-18'250'000
     206 # GWAS     SNPs in this window (with 250'000 flanking)
     194 # SNPs in both (i.e. useful as tags)
   2'144 # target SNPs (anything in narrow window, will include some tags)

chrm1	   18'250'000-18'500'000
     206 # GWAS     SNPs in this window (with 250'000 flanking)
     194 # SNPs in both (i.e. useful as tags)
   2'614 # target SNPs (anything in narrow window, will include some tags)

chrm2	   18'000'000-18'250'000
     156 # GWAS     SNPs in this window (with 250'000 flanking)
     149 # SNPs in both (i.e. useful as tags)
   2'382 # target SNPs (anything in narrow window, will include some tags)

chrm2	   18'250'000-18'500'000
     156 # GWAS     SNPs in this window (with 250'000 flanking)
     149 # SNPs in both (i.e. useful as tags)
   2'700 # target SNPs (anything in narrow window, will include some tags)

chrm3	   18'000'000-18'250'000
      98 # GWAS     SNPs in this window (with 250'000 flanking)
      96 # SNPs in both (i.e. useful as tags)
   1'688 # target SNPs (anything in narrow window, will include some tags)

chrm3	   18'250'000-18'500'000
      98 # GWAS     SNPs in this window (with 250'000 flanking)
      96 # SNPs in both (i.e. useful as tags)
   1'777 # target SNPs (anything in narrow window, will include some tags)

chrm4	   18'000'000-18'250'000
      94 # GWAS     SNPs in this window (with 250'000 flanking)
      90 # SNPs in both (i.e. useful as tags)
   2'224 # target SNPs (anything in narrow window, will include some tags)

chrm4	   18'250'000-18'500'000
      94 # GWAS     SNPs in this window (with 250'000 flanking)
      90 # SNPs in both (i.e. useful as tags)
   2'320 # target SNPs (anything in narrow window, will include some tags)

chrm5	   18'000'000-18'250'000
     102 # GWAS     SNPs in this window (with 250'000 flanking)
     100 # SNPs in both (i.e. useful as tags)
   2'284 # target SNPs (anything in narrow window, will include some tags)

chrm5	   18'250'000-18'500'000
     102 # GWAS     SNPs in this window (with 250'000 flanking)
     100 # SNPs in both (i.e. useful as tags)
   2'750 # target SNPs (anything in narrow window, will include some tags)

chrm6	   18'000'000-18'250'000
     141 # GWAS     SNPs in this window (with 250'000 flanking)
     138 # SNPs in both (i.e. useful as tags)
   2'584 # target SNPs (anything in narrow window, will include some tags)

chrm6	   18'250'000-18'500'000
     141 # GWAS     SNPs in this window (with 250'000 flanking)
     138 # SNPs in both (i.e. useful as tags)
   2'587 # target SNPs (anything in narrow window, will include some tags)

chrm7	   18'000'000-18'250'000
     115 # GWAS     SNPs in this window (with 250'000 flanking)
     103 # SNPs in both (i.e. useful as tags)
   2'106 # target SNPs (anything in narrow window, will include some tags)

chrm7	   18'250'000-18'500'000
     115 # GWAS     SNPs in this window (with 250'000 flanking)
     103 # SNPs in both (i.e. useful as tags)
   2'004 # target SNPs (anything in narrow window, will include some tags)

chrm8	   18'000'000-18'250'000
     180 # GWAS     SNPs in this window (with 250'000 flanking)
     164 # SNPs in both (i.e. useful as tags)
   3'245 # target SNPs (anything in narrow window, will include some tags)

chrm8	   18'250'000-18'500'000
     180 # GWAS     SNPs in this window (with 250'000 flanking)
     164 # SNPs in both (i.e. useful as tags)
   3'263 # target SNPs (anything in narrow window, will include some tags)

chrm9	   18'000'000-18'250'000
     107 # GWAS     SNPs in this window (with 250'000 flanking)
     101 # SNPs in both (i.e. useful as tags)
   2'355 # target SNPs (anything in narrow window, will include some tags)

chrm9	   18'250'000-18'500'000
     107 # GWAS     SNPs in this window (with 250'000 flanking)
     101 # SNPs in both (i.e. useful as tags)
   2'552 # target SNPs (anything in narrow window, will include some tags)

chrm10	   18'000'000-18'250'000
     105 # GWAS     SNPs in this window (with 250'000 flanking)
      97 # SNPs in both (i.e. useful as tags)
     647 # target SNPs (anything in narrow window, will include some tags)

chrm10	   18'250'000-18'500'000
     105 # GWAS     SNPs in this window (with 250'000 flanking)
      97 # SNPs in both (i.e. useful as tags)
   2'792 # target SNPs (anything in narrow window, will include some tags)

chrm11	   18'000'000-18'250'000
     135 # GWAS     SNPs in this window (with 250'000 flanking)
     130 # SNPs in both (i.e. useful as tags)
   2'025 # target SNPs (anything in narrow window, will include some tags)

chrm11	   18'250'000-18'500'000
     135 # GWAS     SNPs in this window (with 250'000 flanking)
     130 # SNPs in both (i.e. useful as tags)
   2'093 # target SNPs (anything in narrow window, will include some tags)

chrm12	   18'000'000-18'250'000
      90 # GWAS     SNPs in this window (with 250'000 flanking)
      86 # SNPs in both (i.e. useful as tags)
   2'185 # target SNPs (anything in narrow window, will include some tags)

chrm12	   18'250'000-18'500'000
      90 # GWAS     SNPs in this window (with 250'000 flanking)
      86 # SNPs in both (i.e. useful as tags)
   2'220 # target SNPs (anything in narrow window, will include some tags)

chrm16	   18'000'000-18'250'000
      59 # GWAS     SNPs in this window (with 250'000 flanking)
      55 # SNPs in both (i.e. useful as tags)
   2'011 # target SNPs (anything in narrow window, will include some tags)

chrm16	   18'250'000-18'500'000
      59 # GWAS     SNPs in this window (with 250'000 flanking)
      55 # SNPs in both (i.e. useful as tags)
     955 # target SNPs (anything in narrow window, will include some tags)

chrm17	   18'000'000-18'250'000
     134 # GWAS     SNPs in this window (with 250'000 flanking)
     125 # SNPs in both (i.e. useful as tags)
   2'131 # target SNPs (anything in narrow window, will include some tags)

chrm17	   18'250'000-18'500'000
     134 # GWAS     SNPs in this window (with 250'000 flanking)
     125 # SNPs in both (i.e. useful as tags)
   1'364 # target SNPs (anything in narrow window, will include some tags)

chrm19	   18'000'000-18'250'000
     176 # GWAS     SNPs in this window (with 250'000 flanking)
     160 # SNPs in both (i.e. useful as tags)
   2'411 # target SNPs (anything in narrow window, will include some tags)

chrm19	   18'250'000-18'500'000
     176 # GWAS     SNPs in this window (with 250'000 flanking)
     160 # SNPs in both (i.e. useful as tags)
   2'336 # target SNPs (anything in narrow window, will include some tags)

chrm20	   18'000'000-18'250'000
     146 # GWAS     SNPs in this window (with 250'000 flanking)
     133 # SNPs in both (i.e. useful as tags)
   2'393 # target SNPs (anything in narrow window, will include some tags)

chrm20	   18'250'000-18'500'000
     146 # GWAS     SNPs in this window (with 250'000 flanking)
     133 # SNPs in both (i.e. useful as tags)
   2'102 # target SNPs (anything in narrow window, will include some tags)

chrm21	   18'000'000-18'250'000
     143 # GWAS     SNPs in this window (with 250'000 flanking)
     134 # SNPs in both (i.e. useful as tags)
   2'219 # target SNPs (anything in narrow window, will include some tags)

chrm21	   18'250'000-18'500'000
     143 # GWAS     SNPs in this window (with 250'000 flanking)
     134 # SNPs in both (i.e. useful as tags)
   2'672 # target SNPs (anything in narrow window, will include some tags)

chrm22	   18'000'000-18'250'000
     185 # GWAS     SNPs in this window (with 250'000 flanking)
     174 # SNPs in both (i.e. useful as tags)
   2'622 # target SNPs (anything in narrow window, will include some tags)

chrm22	   18'250'000-18'500'000
     185 # GWAS     SNPs in this window (with 250'000 flanking)
     174 # SNPs in both (i.e. useful as tags)
   2'551 # target SNPs (anything in narrow window, will include some tags)
