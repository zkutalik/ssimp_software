# COMMANDDIR will already have been defined as the location of the test script
# The '2>&1' on the end is important, to ensure the standard error is captured also
Rscript --vanilla tests/test.wood.R/test.wood.R > "${COMMANDDIR}"/output.actual.tmp 2>&1
