file_name:gwas/too.big/GIANT_HEIGHT_Wood_et_al_2014_publicrelease_HapMapCeuFreq.txt.gz
options:: opt_window_width,options:: opt_flanking_width:	250'000 , 250'000
N_reference:503

chrm19	   15'000'000-15'250'000
     291 # GWAS     SNPs in this window (with 250'000 flanking)
     252 # SNPs in both (i.e. useful as tags)
     693 # target SNPs (anything in narrow window, will include some tags)

chrm19	   15'250'000-15'500'000
     474 # GWAS     SNPs in this window (with 250'000 flanking)
     409 # SNPs in both (i.e. useful as tags)
     450 # target SNPs (anything in narrow window, will include some tags)

chrm19	   15'500'000-15'750'000
     659 # GWAS     SNPs in this window (with 250'000 flanking)
     567 # SNPs in both (i.e. useful as tags)
     577 # target SNPs (anything in narrow window, will include some tags)

chrm19	   15'750'000-16'000'000
     794 # GWAS     SNPs in this window (with 250'000 flanking)
     673 # SNPs in both (i.e. useful as tags)
     923 # target SNPs (anything in narrow window, will include some tags)

chrm19	   16'000'000-16'250'000
     759 # GWAS     SNPs in this window (with 250'000 flanking)
     642 # SNPs in both (i.e. useful as tags)
     775 # target SNPs (anything in narrow window, will include some tags)

chrm19	   16'250'000-16'500'000
     505 # GWAS     SNPs in this window (with 250'000 flanking)
     431 # SNPs in both (i.e. useful as tags)
     526 # target SNPs (anything in narrow window, will include some tags)

chrm19	   16'500'000-16'750'000
     391 # GWAS     SNPs in this window (with 250'000 flanking)
     336 # SNPs in both (i.e. useful as tags)
     502 # target SNPs (anything in narrow window, will include some tags)

chrm19	   16'750'000-17'000'000
     420 # GWAS     SNPs in this window (with 250'000 flanking)
     369 # SNPs in both (i.e. useful as tags)
     572 # target SNPs (anything in narrow window, will include some tags)

chrm19	   17'000'000-17'250'000
     444 # GWAS     SNPs in this window (with 250'000 flanking)
     384 # SNPs in both (i.e. useful as tags)
     653 # target SNPs (anything in narrow window, will include some tags)

chrm19	   17'250'000-17'500'000
     431 # GWAS     SNPs in this window (with 250'000 flanking)
     377 # SNPs in both (i.e. useful as tags)
     610 # target SNPs (anything in narrow window, will include some tags)

chrm19	   17'500'000-17'750'000
     430 # GWAS     SNPs in this window (with 250'000 flanking)
     373 # SNPs in both (i.e. useful as tags)
     576 # target SNPs (anything in narrow window, will include some tags)

chrm19	   17'750'000-18'000'000
     430 # GWAS     SNPs in this window (with 250'000 flanking)
     380 # SNPs in both (i.e. useful as tags)
     684 # target SNPs (anything in narrow window, will include some tags)

chrm19	   18'000'000-18'250'000
     469 # GWAS     SNPs in this window (with 250'000 flanking)
     409 # SNPs in both (i.e. useful as tags)
     570 # target SNPs (anything in narrow window, will include some tags)

chrm19	   18'250'000-18'500'000
     402 # GWAS     SNPs in this window (with 250'000 flanking)
     346 # SNPs in both (i.e. useful as tags)
     552 # target SNPs (anything in narrow window, will include some tags)

chrm19	   18'500'000-18'750'000
     340 # GWAS     SNPs in this window (with 250'000 flanking)
     287 # SNPs in both (i.e. useful as tags)
     515 # target SNPs (anything in narrow window, will include some tags)

chrm19	   18'750'000-19'000'000
     189 # GWAS     SNPs in this window (with 250'000 flanking)
     160 # SNPs in both (i.e. useful as tags)
     468 # target SNPs (anything in narrow window, will include some tags)
